{
  "name": "pm-saas",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "db:create": "wrangler d1 create pm-saas",
    "db:migrate": "wrangler d1 migrations apply pm-saas --local",
    "db:migrate:prod": "wrangler d1 migrations apply pm-saas --remote"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "wrangler": "^4.16.1"
  }
}
